# RevitMCP Server Configuration
# Copy this file to .env and adjust paths for your system

# Required: Directory where your Revit projects are located
MCP_REVIT_WORKSPACE_DIR=C:\RevitProjects

# Required: Directories that the MCP server is allowed to access (semicolon-separated)
MCP_REVIT_ALLOWED_DIRECTORIES=C:\RevitProjects;C:\Users\samo3\Documents

# Bridge URL (default: http://127.0.0.1:3000)
MCP_REVIT_BRIDGE_URL=http://127.0.0.1:3000

# Server mode: "mock" for testing without Revit, "bridge" for real Revit connection
MCP_REVIT_MODE=bridge

# Log level: DEBUG, INFO, WARNING, ERROR
MCP_REVIT_LOG_LEVEL=INFO

# Audit log file path (optional)
MCP_REVIT_AUDIT_LOG=audit.log
